package com.mch.unicoursehub.model.enums;

/**
 * Enum representing the status of a student's enrollment in a course offering.
 */
public enum EnrollmentStatus {
    SELECTED,
    PASSED,
    FAILED,
    DROPPED,
}
